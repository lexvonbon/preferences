# Manifests

## Structure

**`<manifest>`**

```
<manifest config="{stringName}"
          label="{stringID}"
          versionCode="{integer}"
          versionName="{stringName}"
          installLocation=["auto" | "preset" | "env"]
</manifest>
```

## Usage
